services:
  rabbitmq:
    image: rabbitmq:3.13-management
    container_name: cyberflow-rabbitmq
    restart: unless-stopped
    ports:
      - "${RABBITMQ_PORT:-5672}:5672"
      - "${RABBITMQ_MGMT_PORT:-15672}:15672"
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-admin}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASS:-admin123}
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "check_port_connectivity"]
      interval: 10s
      timeout: 5s
      retries: 5

  mysql:
    image: mysql:8.0
    container_name: cyberflow-mysql
    restart: unless-stopped
    ports:
      - "${MYSQL_PORT:-3306}:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-123456}
    volumes:
      - mysql_data:/var/lib/mysql
      - ./script/init_all_databases.sql:/docker-entrypoint-initdb.d/01-init.sql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  db-migrate:
    image: mysql:8.0
    container_name: cyberflow-db-migrate
    environment:
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_USER: ${MYSQL_USER:-root}
      MYSQL_PASSWORD: ${MYSQL_ROOT_PASSWORD:-123456}
      MYSQL_DATABASE: cyberflow
    volumes:
      - ./script/migrations:/migrations:ro
      - ./script/apply_migrations.sh:/usr/local/bin/apply_migrations.sh:ro
    entrypoint: ["/bin/sh", "/usr/local/bin/apply_migrations.sh"]
    depends_on:
      mysql:
        condition: service_healthy

  redis:
    image: redis:7-alpine
    container_name: cyberflow-redis
    restart: unless-stopped
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  backend-admin:
    build: ./backend-admin
    container_name: cyberflow-admin
    restart: unless-stopped
    ports:
      - "${ADMIN_PORT:-8080}:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/cyberflow?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useCursorFetch=true&defaultFetchSize=500
      MYSQL_USER: ${MYSQL_USER:-root}
      MYSQL_PASSWORD: ${MYSQL_ROOT_PASSWORD:-123456}
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: ${RABBITMQ_USER:-admin}
      RABBITMQ_PASS: ${RABBITMQ_PASS:-admin123}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: ${JWT_SECRET:-cyberflow-admin-jwt-secret-key-2026}
      NOTIFICATION_ENCRYPTION_KEY: ${NOTIFICATION_ENCRYPTION_KEY:-${JWT_SECRET:-cyberflow-admin-jwt-secret-key-2026}}
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
      DEEPSEEK_BASE_URL: ${DEEPSEEK_BASE_URL:-https://api.deepseek.com}
      DEEPSEEK_MODEL: ${DEEPSEEK_MODEL:-deepseek-v4-flash}
      RDAP_URL: ${RDAP_URL:-https://rdap.org/domain/{domain}}
      SITE_GENERATION_MAX_ATTEMPTS: ${SITE_GENERATION_MAX_ATTEMPTS:-5}
      CYBERFLOW_PRODUCT_EXPORT_DIRECTORY: /app/product-exports
      SITE_ASSET_DIRECTORY: /app/site-assets
    volumes:
      - product_exports:/app/product-exports
      - site_assets:/app/site-assets
    depends_on:
      db-migrate:
        condition: service_completed_successfully
      rabbitmq:
        condition: service_healthy
      mysql:
        condition: service_healthy

  crawler-consumer:
    build:
      context: .
      dockerfile: crawler-consumer/Dockerfile
    container_name: cyberflow-consumer
    restart: unless-stopped
    environment:
      DATABASE_URL: mysql+pymysql://${MYSQL_USER:-root}:${MYSQL_ROOT_PASSWORD:-123456}@mysql:3306/cyberflow
      SCRAPED_DB_URL: mysql+pymysql://${MYSQL_USER:-root}:${MYSQL_ROOT_PASSWORD:-123456}@mysql:3306/scraped_data
      REDIS_URL: redis://redis:6379/0
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: ${RABBITMQ_USER:-admin}
      RABBITMQ_PASS: ${RABBITMQ_PASS:-admin123}
      ADMIN_API_BASE_URL: ${ADMIN_API_BASE_URL:-}
      ADMIN_API_USERNAME: ${ADMIN_API_USERNAME:-}
      ADMIN_API_PASSWORD: ${ADMIN_API_PASSWORD:-}
      PAYMENT_API_BASE_URL: ${PAYMENT_API_BASE_URL:-}
      PAYMENT_API_ACCOUNT: ${PAYMENT_API_ACCOUNT:-}
      PAYMENT_API_PASSWORD: ${PAYMENT_API_PASSWORD:-}
      VERIFY_SSL: ${VERIFY_SSL:-true}
      SCRAPY_PROJECT_PATH: /app/scrapy_app
      PRODUCT_CRAWL_TIMEOUT_SECONDS: ${PRODUCT_CRAWL_TIMEOUT_SECONDS:-7200}
      PRODUCT_CRAWL_PROXY_URL: ${PRODUCT_CRAWL_PROXY_URL:-}
      PRODUCT_DOMAIN_CURRENCIES: ${PRODUCT_DOMAIN_CURRENCIES:-}
      PRODUCT_DISCOVERY_MAX_PAGES: ${PRODUCT_DISCOVERY_MAX_PAGES:-100}
      PRODUCT_CRAWL_CA_BUNDLE: ${PRODUCT_CRAWL_CA_BUNDLE:-}
      PRODUCT_REDIS_ENABLED: ${PRODUCT_REDIS_ENABLED:-true}
      PRODUCT_PLATFORM_CONFIGS: ${PRODUCT_PLATFORM_CONFIGS:-}
    depends_on:
      db-migrate:
        condition: service_completed_successfully
      rabbitmq:
        condition: service_healthy
      mysql:
        condition: service_healthy

volumes:
  product_exports:
  site_assets:
  rabbitmq_data:
  mysql_data:
  redis_data:
