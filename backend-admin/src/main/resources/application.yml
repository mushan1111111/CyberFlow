server:
  port: 8080

spring:
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      connect-timeout: 2s
      timeout: 2s

  sql:
    init:
      mode: always
      encoding: UTF-8

  datasource:
    url: jdbc:mysql://localhost:3306/cyberflow?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useCursorFetch=true&defaultFetchSize=500
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:123456}
    driver-class-name: com.mysql.cj.jdbc.Driver

  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:admin}
    password: ${RABBITMQ_PASS:admin123}
    listener:
      simple:
        retry:
          enabled: true
          max-attempts: 3
        default-requeue-rejected: false

  quartz:
    job-store-type: memory
    properties:
      org:
        quartz:
          scheduler:
            instanceName: CyberFlowScheduler
          threadPool:
            threadCount: 3

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

springdoc:
  api-docs:
    path: /admin/api-docs
  swagger-ui:
    path: /admin/swagger-ui.html

cyberflow:
  site-assets:
    directory: ${SITE_ASSET_DIRECTORY:.data/site-assets}
  site-generation:
    # Keep the key in the deployment environment; never commit it to the repository.
    deepseek-api-key: ${DEEPSEEK_API_KEY:}
    deepseek-base-url: ${DEEPSEEK_BASE_URL:https://api.deepseek.com}
    deepseek-model: ${DEEPSEEK_MODEL:deepseek-v4-flash}
    rdap-url: ${RDAP_URL:https://rdap.org/domain/{domain}}
    max-attempts: ${SITE_GENERATION_MAX_ATTEMPTS:5}
    prompt: ${SITE_GENERATION_PROMPT:}
  crawler:
    site-cron: "0 0 */6 * * ?"     # 从00:00开始，每6小时整点执行
    index-cron: "0 0 0 * * ?"      # 每天00:00采集站点收录快照
    order-cron: "0 0 */6 * * ?"    # 从00:00开始，每6小时整点执行
    site-account-cron: "0 30 3 ? * MON"  # 每周一 03:30 按每个人的账号同步本人站点主题与分类
  jwt:
    secret: ${JWT_SECRET:cyberflow-admin-jwt-secret-key-2026}
    expiration: 14400000
