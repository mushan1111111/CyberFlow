# CyberFlow Docker environment variables
# Copy this file to .env and replace all change-me values before deployment.

# MySQL
MYSQL_ROOT_PASSWORD=change-me
MYSQL_USER=root

# RabbitMQ
RABBITMQ_USER=admin
RABBITMQ_PASS=change-me

# JWT
JWT_SECRET=change-me-to-a-long-random-string
# Encrypts notification Webhooks and signing secrets stored in MySQL.
NOTIFICATION_ENCRYPTION_KEY=change-me-to-another-long-random-string

# AI-assisted new-site generation
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-flash
RDAP_URL=https://rdap.org/domain/{domain}
SITE_GENERATION_MAX_ATTEMPTS=5

# Crawler platform fallbacks (admin settings take precedence)
ADMIN_API_BASE_URL=
ADMIN_API_USERNAME=
ADMIN_API_PASSWORD=
PAYMENT_API_BASE_URL=
PAYMENT_API_ACCOUNT=
PAYMENT_API_PASSWORD=
VERIFY_SSL=true
# Allow large product catalogs to finish; value is in seconds.
PRODUCT_CRAWL_TIMEOUT_SECONDS=7200
PRODUCT_CRAWL_PROXY_URL=
PRODUCT_DOMAIN_CURRENCIES={}
PRODUCT_DISCOVERY_MAX_PAGES=100
PRODUCT_CRAWL_CA_BUNDLE=
PRODUCT_REDIS_ENABLED=true
# Per-domain API settings; keep real tokens in the local env file only.
PRODUCT_PLATFORM_CONFIGS={}

# Host ports (uncomment to override defaults)
# ADMIN_PORT=8080
# RABBITMQ_PORT=5672
# RABBITMQ_MGMT_PORT=15672
# MYSQL_PORT=3306
# REDIS_PORT=6379
